# 0.6 - New Note in Single page app diagram

``` mermaid
---
config:
  layout: elk
---
flowchart TD
    U(["User types note & presses Save"]) --> B1["SPA intercepts event"] & n1["Untitled Node"]
    B1 --> B2["JS builds new note object with content and date"]
    B2 --> B3["Update UI immediately and then render"]
    B2 -- POST new note to server with JSON body --> S["Server"]
    S -- Store and reply with 201 Created --> B2
    B3 --> U2(["User sees new note instantly, no reload needed"])
```
