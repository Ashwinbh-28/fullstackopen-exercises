# 0.5 - New single page app diagram

``` mermaid
flowchart TD
    B[Browser] -->|GET /spa| S[Server]
    S -->|HTML shell SPA| B

    B -->|GET /main.css| S
    S -->|CSS styles| B

    B -->|GET /spa.js| S
    S -->|SPA JavaScript| B

    B -->|Run JS logic| B2[Attach event listeners + request notes]

    B2 -->|GET /data.json| S
    S -->|JSON notes list| B2

    B2 --> U([UI shows notes without reloading page])
```
